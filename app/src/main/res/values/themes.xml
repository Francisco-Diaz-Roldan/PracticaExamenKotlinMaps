<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PracticaExamen" parent="Theme.Material3.DayNight">
        <item name="android:textColorPrimary">@color/orange</item>
    </style>
    <!-- Personalización de la barra de herramientas (Toolbar) -->
    <style name="ToolbarThemeExamen" parent="Widget.Material3.Toolbar">
        <item name="android:background">@color/orange</item> <!-- Cambio el fondo de la Toolbar a verde -->
        <item name="titleTextAppearance">@style/ToolbarTitleTextExamen</item> <!-- Estilo del texto del título -->
    </style>

    <style name="ToolbarTitleTextExamen" parent="TextAppearance.Material3.ActionBar.Title">
        <item name="android:textColor">@color/white</item> <!-- Color del texto del título (blanco) -->
    </style>

    <style name="Theme.PracticaExamen" parent="Base.Theme.PracticaExamen">
        <item name="toolbarStyle">@style/ToolbarThemeExamen</item> <!-- Aplico la Toolbar personalizada-->
    </style>
</resources>